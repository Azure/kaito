apiVersion: v1
kind: Secret
metadata:
  name: webhook-cert
  namespace: {{ include "kaito.fullname" .}}
  labels:
    {{- include "kaito.labels" . | nindent 4 }}
data:
   server-key.pem: ""
   server-cert.pem: ""
   ca-cert.pem: ""
