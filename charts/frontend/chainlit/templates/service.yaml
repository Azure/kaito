apiVersion: v1
kind: Service
metadata:
  name: {{ include "chainlit.fullname" . }}
  labels:
    {{- include "chainlit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chainlit.selectorLabels" . | nindent 4 }}
